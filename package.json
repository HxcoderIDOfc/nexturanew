{
  "name": "nextura-whatsapp-bot",
  "version": "1.1.0",
  "description": "Nextura WhatsApp bot dengan QR login, persistent session path, plugin manager, plugin autoload, dan Nextura AI chat.",
  "type": "module",
  "main": "start.js",
  "scripts": {
    "start": "node start.js",
    "dev": "node --watch start.js",
    "check": "node --check start.js && node --check sdk-compat.js && node --check whatsapp-bot.js && node --check wa-plugins/ping.js && node --check wa-plugins/ai-chat.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "^6.7.18",
    "qrcode": "^1.5.4"
  }
}
