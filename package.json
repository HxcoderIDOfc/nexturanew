{
  "name": "nextura-ai-router-koyeb",
  "version": "3.0.0",
  "description": "Nextura AI Router untuk Koyeb dengan adaptive Cortexa Max Engine, OpenAI SDK, Anthropic SDK, Nextura SDK, thinking, web, vision, sandbox terminal, PDF, download, web screenshot, typo detection, dan public location resolver",
  "type": "module",
  "main": "location-bootstrap.js",
  "scripts": {
    "start": "node location-bootstrap.js",
    "start:sdk": "node sdk-compat.js",
    "start:gateway": "node nextura-gateway.js",
    "start:koyeb": "node koyeb.js",
    "start:router": "node server.js",
    "start:terminal": "node terminal-tool.js",
    "dev": "node --watch location-bootstrap.js",
    "check": "node --check location-bootstrap.js && node --check sdk-compat.js && node --check sdk/nextura.js && node --check max-engine.js && node --check terminal-agent.js && node --check terminal-agent-v2.js && node --check nextura-gateway.js && node --check terminal-tool.js && node --check terminal-tool-v2.js && node --check sandbox-artifacts.js && node --check koyeb-base.js && node --check koyeb.js && node --check server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/rate-limit": "^10.2.1",
    "fastify": "^5.2.1",
    "pdf-lib": "^1.17.1",
    "puppeteer-core": "^24.16.0"
  }
}
